//
// Created by pospelov on 04.02.2021.
//

#include "Settings.hpp"

Settings::Settings(const sf::VideoMode &video_mode, int windowStyle, const sf::Time &time_per_frame) : videoMode(video_mode), windowStyle(windowStyle), TIME_PER_FRAME(time_per_frame) {
}
